Learnings:
============================

1). Creating a flag column
2). concat two columns for flag and using > / < to compare
3). row_number() window function
4). Using multiple cte
5). Join and using multiple alis for join statements
6). case statements
7). 3 joins in a go, with order of execution in joining
8). Cross join and its use case
9). first_value() window function
10). string_agg()

